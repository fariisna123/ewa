---
kind: pipeline
type: docker
name: Building
node:
  drone: bzkg9
trigger:
  event:
    include:
      - tag
steps:
  - name: build alpine
    image: thegeeklab/drone-docker-buildx:23
    privileged: true
    settings:
      username: smartinfrasolutions
      dockerfile: Dockerfile-alpine
      platforms:
        - linux/amd64
        - linux/arm64
